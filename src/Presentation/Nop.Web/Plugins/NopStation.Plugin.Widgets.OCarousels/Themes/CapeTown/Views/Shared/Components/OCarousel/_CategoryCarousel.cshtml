@model OCarouselListModel.OCarouselOverviewModel

@if (Model.ShowBackgroundPicture)
{
    <div class="custom-container category-carousel" style="background:url('@Model.BackgroundPictureUrl') no-repeat center; background-size: cover;">
        <div class="ocarousel-grid home-page-category-grid">
            @if (Model.DisplayTitle)
            {
                <div class="title">
                    <strong>@Model.Title</strong>
                </div>
            }

            <div class="carousel-container carousel-category" data-carouselid="@Model.Id">
                <div class="loader">
                    <div class="lds-facebook"><div></div><div></div><div></div></div>
                </div>
            </div>
        </div>
    </div>
}
else
{
    <div class="custom-container category-carousel">
        <div class="category-grid">
            @if (Model.DisplayTitle)
            {
                <div class="title">
                    <strong>@Model.Title</strong>
                </div>
            }

            <div class="carousel-container carousel-category" data-carouselid="@Model.Id">
                <div class="loader">
                    <div class="lds-facebook"><div></div><div></div><div></div></div>
                </div>
            </div>
        </div>
    </div>
}

