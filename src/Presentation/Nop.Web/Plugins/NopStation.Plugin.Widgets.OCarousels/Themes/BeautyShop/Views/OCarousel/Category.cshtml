@model OCarouselModel

@if (Model.Categories.Count > 0)
{
    <div class="owl-carousel ocarousel @Model.CustomCssClass" id="category-ocarousel-@(Model.Id)">
        @foreach (var categoryModel in Model.Categories)
        {
            <div class="ocarousel-item">
                <a href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = categoryModel.SeName }))" title="@categoryModel.PictureModel.Title">
                    <img src="@categoryModel.PictureModel.ImageUrl" alt="@categoryModel.PictureModel.AlternateText" />
                </a>
            </div>
            <div class="category-details beautyShop-overlay">
                <a class="beautyShop-secondery-color-text beautyShop-medium-title" href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = categoryModel.SeName }))">@categoryModel.Name</a>
                <a class="shop-now" href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = categoryModel.SeName }))">@T("shop.now")</a>
            </div>
        }
    </div>

    <script>
        $(document).ready(function () {
            $('#category-ocarousel-@(Model.Id)').owlCarousel({
                nav: @Model.Nav.ToString().ToLower(),
                loop: @Model.Loop.ToString().ToLower(),
                dots: true,
                center: true,
                margin: 10,
                autoplay: @Model.AutoPlay.ToString().ToLower(),
                rtl: @Model.Rtl.ToString().ToLower(),
                autoplayTimeout: @Model.AutoPlayTimeout,
                autoplayHoverPause: @Model.AutoPlayHoverPause.ToString().ToLower(),
                lazyLoad: @Model.LazyLoad.ToString().ToLower(),
                lazyLoadEager: @Model.LazyLoadEager,
                center: @Model.Center.ToString().ToLower(),
                startPosition: @Model.StartPosition,
                responsive: {
                    0: {
                        items: 1
                    },
                    576: {
                        items: 2
                    },
                    768: {
                        items: 2
                    },
                    992: {
                        items: 3
                    },
                    1200: {
                        items: 3
                    },
                    1500: {
                        items: 3
                    }
                }
            });
        })
    </script>
}