@model ProductDetailsModel
@if (Model.DisplayBackInStockSubscription)
{
    <div class="back-in-stock-subscription">
        <input type="button" id="back-in-stock-subscribe-@Model.Id" class="button-2 subscribe-button" value="@T("BackInStockSubscriptions.NotifyMeWhenAvailable")" />
        <script asp-location="Footer">
            $(document).ready(function () {
                $("#back-in-stock-subscribe-@Model.Id").on('click', function () {
                    displayPopupContentFromUrl('@Url.RouteUrl(NopRouteNames.Ajax.BACK_IN_STOCK_SUBSCRIBE_POPUP, new { productId = Model.Id })', '@T("BackInStockSubscriptions.NotifyMeWhenAvailable")');
                });
            });
        </script>
    </div>
}