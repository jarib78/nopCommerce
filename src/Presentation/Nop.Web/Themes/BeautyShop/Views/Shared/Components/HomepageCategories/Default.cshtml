@model IList<CategoryModel>
@if (Model.Count > 0)
{
    <div class="home-page-category-grid">
        <div class="item-grid">
            @{var categoryCount = 0;}
            @foreach (var item in Model)
            {
                if (categoryCount < 8)
                {
                    <div class="item-box">
                        <div class="category-item">
                            <h2 class="title category-title">
                                <a href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = item.SeName }))" title="@item.PictureModel.Title">
                                    <span>@item.Name</span>
                                </a>
                            </h2>
                            <div class="picture" style="background-image:url(@item.PictureModel.ImageUrl)">
                                <a href="@(await NopUrl.RouteGenericUrlAsync<Category>(new { SeName = item.SeName }))" title="@item.PictureModel.Title">
                                    @*<img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />*@
                                </a>
                            </div>
                        </div>
                    </div>
                }

                categoryCount++;
            }
        </div>
    </div>
}