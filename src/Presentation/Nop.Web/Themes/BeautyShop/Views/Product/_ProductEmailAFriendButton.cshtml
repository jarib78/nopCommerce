@model ProductDetailsModel
@if (Model.EmailAFriendEnabled)
{
    <div class="email-a-friend">
        <button  title="@T("Products.EmailAFriend")" type="button" class="button-2 email-a-friend-button beautyShop-primary-color-hover" onclick="setLocation('@Url.RouteUrl(NopRouteNames.Standard.PRODUCT_EMAIL_FRIEND, new { productId = Model.Id })')">@T("Products.EmailAFriend")</button>
    </div>
}
